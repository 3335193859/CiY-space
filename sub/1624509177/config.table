{
  ["web"]	= {
      ["默认广告标记标签"]	= "" ;
      ["浏览器用户标识"]	= "默认" ;
      ["启用进度条"]	= true ;
      ["支持打开外部应用"]	= true ;
      ["启用长按菜单"]	= true ;
      ["网页沉浸"]	= false ;
      ["惰性加载"]	= true ;
      } ;
  ["sidebar"]	= {
      ["items"]	= {
          } ;
      ["启用"]	= false ;
      ["启用顶部图片"]	= false ;
      } ;
  ["bottomBar"]	= {
      [1]	= {
          ["icon"]	= "drawable/home.png" ;
          ["text"]	= "首页" ;
          ["url"]	= "http://ciyuandao.com" ;
          ["statusBarColor"]	= "" ;
          } ;
      [2]	= {
          ["icon"]	= "drawable/camera_party_mode.png" ;
          ["text"]	= "COSER广场" ;
          ["url"]	= "http://ciyuandao.com/cosers" ;
          ["statusBarColor"]	= "" ;
          } ;
      [3]	= {
          ["icon"]	= "drawable/home_modern.png" ;
          ["text"]	= "社团" ;
          ["url"]	= "http://ciyuandao.com/teams" ;
          ["statusBarColor"]	= "" ;
          } ;
      [4]	= {
          ["icon"]	= "drawable/folder_multiple_image.png" ;
          ["text"]	= "图集" ;
          ["url"]	= "http://ciyuandao.com/photo" ;
          ["statusBarColor"]	= "" ;
          } ;
      [5]	= {
          ["icon"]	= "drawable/beach.png" ;
          ["text"]	= "活动" ;
          ["url"]	= "http://ciyuandao.com/activities" ;
          ["statusBarColor"]	= "" ;
          } ;
      [6]	= {
          ["icon"]	= "drawable/account_box.png" ;
          ["text"]	= "登录" ;
          ["url"]	= "http://ciyuandao.com/login" ;
          ["statusBarColor"]	= "" ;
          } ;
      } ;
  ["colors"]	= {
      ["侧滑栏列表文字"]	= "#ff333333" ;
      ["导航栏"]	= "#ff2c2e43" ;
      ["顶栏部件"]	= "#ffff5d8a" ;
      ["悬浮按钮图标"]	= "#FFFFFF" ;
      ["底栏图标选中"]	= "#ffff5d8a" ;
      ["底栏"]	= "#ffffffff" ;
      ["搜索栏部件触摸"]	= "#44000000" ;
      ["搜索栏部件"]	= "#ff888888" ;
      ["进度条"]	= "#ffff5d8a" ;
      ["底栏图标触摸波纹"]	= "#24000000" ;
      ["搜索栏背景"]	= "#ffffffff" ;
      ["顶栏部件触摸"]	= "#22000000" ;
      ["搜索栏提示文字"]	= "#80000000" ;
      ["侧滑栏列表图标"]	= "#ff757575" ;
      ["底栏图标"]	= "#ff17a4ff" ;
      ["侧滑栏背景"]	= "#ffffffff" ;
      ["顶栏"]	= "#ff2eafff" ;
      } ;
  ["toolbar"]	= {
      ["搜索框提示"]	= "" ;
      ["搜索功能"]	= false ;
      ["menus"]	= {
          [1]	= {
              ["name"]	= "退出" ;
              ["onClick"]	= "退出页面()" ;
              } ;
          } ;
      ["搜索URL"]	= "" ;
      ["输入栏模式"]	= false ;
      ["icon_btns"]	= {
          [1]	= {
              ["icon"]	= "replay" ;
              ["onClick"]	= "刷新网页()" ;
              } ;
          } ;
      ["启用搜索功能"]	= false ;
      } ;
  ["web_control"]	= {
      [1]	= {
          ["remove_element"]	= "wbox,wbox_c foot,in_right fright" ;
          ["js"]	= "" ;
          ["url"]	= "ciyuandao.com" ;
          } ;
      } ;
  ["switch"]	= {
      ["支持滑动页面"]	= true ;
      ["显示状态栏"]	= true ;
      ["横屏"]	= false ;
      ["启用启动图"]	= false ;
      } ;
  ["global_js"]	= "\
/**\
 * 禁用动态添加脚本，防止广告加载\
 *\
 * @param valid bool? true(valid)|false(invalid)|other(off)\
 * @param rule array 配置允许(valid)|不允许(invalid)的脚本规则：支持regex、string、function\
 */\
(function(valid, rule) {\
    if(typeof Element === 'undefined') console.log('IE8以下浏览器无效');\
    var origin = new RegExp('^' + location.origin),Ele = Element;\
    each(['appendChild', 'insertBefore', 'insertAfter'], proxy);\
\
    function proxy(prop) {\
        var proxy_obj = Ele.prototype[prop];\
        Ele.prototype[prop] = function(elem) {\
            if (!elem.children.length) {\
                var tag = elem.tagName.toLowerCase();\
                if (tag == 'script' && isBanScript(elem)) {\
                    console.log('禁用脚本：' + elem.src);\
                    var substitute = document.createElement('script');\
                    substitute.innerHTML = '// 禁用脚本：' + elem.src;\
                    elem = substitute;\
                }\
            }\
            return proxy_obj.apply(this, arguments);\
        };\
    }\
\
    function isBanScript(script) {\
        if (origin.test(script.src)) return false;\
        return valid === each(rule, match);\
\
        function match(val) {\
            var type = typeof val;\
            if (type === 'string') {\
                if (script.src == val) return true;\
            } else if (type === 'function') {\
                if (val(script)) return true;\
            } else {\
                if (val.test(script.src)) return true;\
            }\
            return false;\
        }\
    }\
\
    function each(arr, fn) {\
        if (arr) {\
            for (var i = 0, n = arr.length; i < n; i++) {\
                if (fn.call(arr[i], arr[i], i) === true) return false;\
            }\
        }\
        return true;\
    }\
})(true, []);\
//表示有效的脚本规则列表\
\
\
" ;
  ["view_attribute"]	= {
      ["顶栏高度"]	= "56dp" ;
      ["顶栏标题"]	= "次元岛" ;
      ["底栏图标大小"]	= "24dp" ;
      ["底栏高度"]	= "48dp" ;
      ["侧滑栏宽度"]	= "240dp" ;
      ["顶栏阴影强度"]	= "2dp" ;
      ["搜索栏阴影强度"]	= "2dp" ;
      ["搜索栏圆角弧度"]	= "2dp" ;
      } ;
  ["events"]	= {
      ["网页即将加载"]	= "--浏览器即将加载新的网页时执行的事件\
\
--接收参数\
浏览器对象,网页链接=..." ;
      ["网页加载完毕"]	= "--网页加载完毕后执行的事件\
\
--接收参数\
浏览器对象,网页链接=..." ;
      ["程序启动"]	= "--程序启动时会执行的事件" ;
      ["收到新标题"]	= "--浏览器加载新页面并获得新标题时执行的事件\
\
--接收参数\
新标题=..." ;
      } ;
  } ;